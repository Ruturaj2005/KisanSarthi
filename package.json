{
  "name": "kisansaathi",
  "version": "1.0.0",
  "description": "KisanSaathi — Farmer's Companion: Democratizing precision agriculture for India's farmers",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c green,blue \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:ml": "cd ml-service && uvicorn app.main:app --reload --port 8000",
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "seed": "node backend/src/scripts/seed.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
